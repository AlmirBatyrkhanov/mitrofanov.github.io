# 📖 Руководство по использованию

## 🚀 Быстрый старт

### Windows
```bash
# Двойной клик на файл
start.bat

# Или через командную строку
python main.py
```

### Linux/macOS
```bash
# Сделать исполняемым и запустить
chmod +x start.sh
./start.sh

# Или напрямую
python3 main.py
```

## 🌐 Доступные страницы

- **Главная**: http://localhost:8000
- **Демо**: http://localhost:8000/demo.html
- **API Health**: http://localhost:8000/api/health
- **API Info**: http://localhost:8000/api/info

## 📧 Настройка email

### 1. Создайте файл `.env`
```env
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
RECIPIENT_EMAIL=almir@example.com
```

### 2. Настройка Gmail
1. Включите двухфакторную аутентификацию
2. Создайте пароль приложения
3. Используйте этот пароль в `SMTP_PASSWORD`

### 3. Другие провайдеры
- **Yandex**: `smtp.yandex.ru:587`
- **Mail.ru**: `smtp.mail.ru:587`
- **Outlook**: `smtp-mail.outlook.com:587`

## 🤖 Настройка Telegram

### 1. Создайте бота
1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Получите токен бота

### 2. Узнайте chat_id
1. Найдите [@userinfobot](https://t.me/userinfobot)
2. Отправьте любое сообщение
3. Скопируйте ваш chat_id

### 3. Добавьте в `.env`
```env
TELEGRAM_BOT_TOKEN=your-bot-token
TELEGRAM_CHAT_ID=your-chat-id
```

## 🎨 Кастомизация

### Изменение цветов
Отредактируйте переменные в `styles.css`:
```css
:root {
    --accent-blue: #00d4ff;
    --accent-purple: #8b5cf6;
    --accent-teal: #14b8a6;
    --accent-pink: #ec4899;
}
```

### Изменение контента
1. **Личная информация**: `index.html` и `main.py`
2. **Проекты**: Секция "Projects" в `index.html`
3. **Навыки**: Секция "Skills" в `index.html`
4. **Контакты**: Секция "Contact" в `index.html`

### Добавление анимаций
Используйте классы из `animations.css`:
```css
.fade-in          /* Появление снизу */
.slide-left       /* Появление слева */
.slide-right      /* Появление справа */
.scale-in         /* Масштабирование */
.rotate-in        /* Появление с поворотом */
```

## 🔧 Разработка

### Структура проекта
```
├── Frontend (HTML/CSS/JS)
│   ├── index.html      # Главная страница
│   ├── demo.html       # Демо страница
│   ├── styles.css      # Основные стили
│   ├── animations.css  # Анимации
│   └── script.js       # JavaScript
│
├── Backend (Python/FastAPI)
│   ├── main.py         # API сервер
│   └── requirements.txt # Зависимости
│
└── Документация
    ├── README.md
    ├── QUICKSTART.md
    ├── PROJECT_INFO.md
    └── USAGE.md
```

### Добавление новых секций
1. Добавьте HTML в `index.html`
2. Добавьте стили в `styles.css`
3. Добавьте анимации в `script.js`
4. Обновите навигацию

### Добавление новых API endpoints
```python
@app.get("/api/new-endpoint")
async def new_endpoint():
    return {"message": "Hello World"}
```

## 🚀 Деплой

### Vercel (Frontend)
1. Подключите GitHub репозиторий
2. Настройте build команду
3. Деплой автоматический

### Heroku (Backend)
1. Создайте `Procfile`:
```
web: uvicorn main:app --host 0.0.0.0 --port $PORT
```
2. Добавьте переменные окружения
3. Деплой через Git

### Docker
```dockerfile
FROM python:3.9-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
EXPOSE 8000
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

## 🐛 Решение проблем

### Сервер не запускается
1. Проверьте версию Python (3.8+)
2. Установите зависимости: `pip install -r requirements.txt`
3. Проверьте порт 8000 (может быть занят)

### Email не отправляется
1. Проверьте настройки в `.env`
2. Убедитесь в правильности SMTP данных
3. Проверьте пароль приложения для Gmail

### Telegram не работает
1. Проверьте токен бота
2. Проверьте chat_id
3. Убедитесь, что бот не заблокирован

### Анимации не работают
1. Проверьте подключение `animations.css`
2. Убедитесь в поддержке CSS в браузере
3. Проверьте JavaScript консоль на ошибки

## 📱 Тестирование

### Локальное тестирование
```bash
# Запуск сервера
python main.py

# Тестирование API
curl http://localhost:8000/api/health
curl http://localhost:8000/api/info

# Тестирование формы
curl -X POST http://localhost:8000/api/contact \
  -H "Content-Type: application/json" \
  -d '{"name":"Test","email":"test@example.com","message":"Hello"}'
```

### Мобильное тестирование
1. Запустите сервер
2. Найдите IP адрес компьютера
3. Откройте `http://YOUR_IP:8000` на мобильном устройстве

## 📊 Мониторинг

### Логи сервера
Сервер выводит логи в консоль:
- Запуск и остановка
- HTTP запросы
- Ошибки email/Telegram
- Статус API

### Проверка состояния
```bash
# Проверка API
curl http://localhost:8000/api/health

# Проверка информации
curl http://localhost:8000/api/info
```

## 🔒 Безопасность

### Рекомендации
1. Не коммитьте файл `.env` в Git
2. Используйте HTTPS в продакшене
3. Ограничьте CORS для продакшена
4. Валидируйте все входные данные

### Переменные окружения
```env
# Обязательные
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
RECIPIENT_EMAIL=almir@example.com

# Опциональные
TELEGRAM_BOT_TOKEN=your-bot-token
TELEGRAM_CHAT_ID=your-chat-id
```

---

*Для получения помощи обращайтесь к документации или создавайте issues в репозитории*
