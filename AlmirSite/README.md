# Almir Mitrofanov - Personal Portfolio Website

Современный персональный сайт-визитка с премиальным тёмным дизайном и эффектом стекла (glassmorphism).

## 🚀 Особенности

- **Современный дизайн**: Тёмная тема с эффектами glassmorphism
- **Адаптивная вёрстка**: Оптимизирован для всех устройств
- **Плавные анимации**: Современные переходы и эффекты
- **Интерактивность**: Динамические элементы и hover-эффекты
- **Backend API**: FastAPI для обработки контактной формы
- **Уведомления**: Email и Telegram уведомления

## 🛠 Технологии

### Frontend
- HTML5, CSS3, JavaScript (ES6+)
- Font Awesome иконки
- Google Fonts (Inter)
- CSS Grid и Flexbox
- CSS Animations и Transitions

### Backend
- Python 3.8+
- FastAPI
- Uvicorn
- SMTP для email
- Telegram Bot API

## 📦 Установка и запуск

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd almir-portfolio
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения
Создайте файл `.env` в корневой папке:

```env
# Email Configuration
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
RECIPIENT_EMAIL=almir@example.com

# Telegram Bot Configuration (Optional)
TELEGRAM_BOT_TOKEN=your-bot-token
TELEGRAM_CHAT_ID=your-chat-id
```

### 4. Запуск сервера
```bash
python main.py
```

Сайт будет доступен по адресу: `http://localhost:8000`

## 📧 Настройка Email

### Gmail
1. Включите двухфакторную аутентификацию
2. Создайте пароль приложения
3. Используйте этот пароль в переменной `SMTP_PASSWORD`

### Другие провайдеры
Обновите настройки SMTP в файле `.env`:
- **Yandex**: `smtp.yandex.ru:587`
- **Mail.ru**: `smtp.mail.ru:587`
- **Outlook**: `smtp-mail.outlook.com:587`

## 🤖 Настройка Telegram Bot

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота
3. Узнайте свой chat_id (можно через [@userinfobot](https://t.me/userinfobot))
4. Добавьте токен и chat_id в `.env`

## 🎨 Кастомизация

### Цветовая схема
Основные цвета определены в CSS переменных:
```css
:root {
    --accent-blue: #00d4ff;
    --accent-purple: #8b5cf6;
    --accent-teal: #14b8a6;
    --accent-pink: #ec4899;
}
```

### Контент
Обновите информацию в файлах:
- `index.html` - основной контент
- `main.py` - информация в API

## 📱 Адаптивность

Сайт оптимизирован для:
- Десктоп (1200px+)
- Планшет (768px - 1199px)
- Мобильные (до 767px)

## 🚀 Деплой

### Vercel (Frontend)
1. Подключите репозиторий к Vercel
2. Настройте build команду: `npm run build`
3. Деплой автоматический

### Heroku (Backend)
1. Создайте `Procfile`:
```
web: uvicorn main:app --host 0.0.0.0 --port $PORT
```
2. Добавьте переменные окружения в настройках Heroku
3. Деплой через Git

### Docker
```dockerfile
FROM python:3.9-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
EXPOSE 8000
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

## 📄 Структура проекта

```
almir-portfolio/
├── index.html          # Главная страница
├── styles.css          # Стили
├── script.js           # JavaScript
├── main.py             # FastAPI сервер
├── requirements.txt    # Python зависимости
└── README.md           # Документация
```

## 🔧 API Endpoints

- `GET /` - Главная страница
- `POST /api/contact` - Отправка сообщения
- `GET /api/health` - Проверка состояния
- `GET /api/info` - Информация о разработчике

## 📝 Лицензия

Этот проект создан для демонстрации навыков разработки. Все права защищены.

## 👨‍💻 Автор

**Almir Mitrofanov**
- 15 лет
- Full-Stack Web Developer
- Bot & Parser Creator

---

*Создано с ❤️ и современными технологиями*
